## Potential approach

The tasks can be solved in multiple methods bu the best possible method is to keep a base query that fetched all the data from the table then based in the user interaction,user selection uploaded to the button build the final sql query that can be executed to filter the data and display it in the ui this is the general logic for displaying data.

THe export section
